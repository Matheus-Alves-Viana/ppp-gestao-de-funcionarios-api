name: CI PPP Gestão de Funcionário

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: "0 3 * * *"

jobs:
  tests:
    runs-on: ubuntu-latest

    env:
      BASE_URL: 'http://localhost:3000'

    steps:
      - name: 📥 Checkout do repositório
        uses: actions/checkout@v4

      - name: 📦 Instalar dependências
        run: |
          npm install
          npm install --save-dev chai mocha supertest wait-on

      - name: ▶️ Iniciar servidor em background
        run: |
          npm start &
          echo "⏳ Aguardando o servidor iniciar..."
          npx wait-on http-get://localhost:3000/api/gerente
        env:
          NODE_ENV: test

      - name: 🧪 Executar os testes
        run: npm test
        env:
          NODE_ENV: test

      - name: 🛑 Encerrar servidor
        if: always()
        run: pkill node || true
