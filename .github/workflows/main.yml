name: CI PPP Gestão de Funcionários

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    # Executa diariamente às 03:00 UTC (00:00 em Brasília)
    - cron: "0 3 * * *"

jobs:
  tests:
    runs-on: ubuntu-latest

    env:
      BASE_URL: 'http://localhost:3000'
      NODE_ENV: 'test'
      JWT_SECRET: 'segredo_teste'

    steps:
      - name: 📥 Checkout do repositório
        uses: actions/checkout@v4

      - name: 🧰 Instalar dependências
        run: npm install

      - name: 🧩 Instalar wait-on (para aguardar o servidor)
        run: npm install -g wait-on

      - name: 🚀 Iniciar servidor da aplicação em background
        run: |
          nohup npm start &
          npx wait-on http://localhost:3000
        shell: bash

      - name: 🧪 Executar testes (Mocha + Supertest + Mochawesome)
        run: npm test

      - name: 💾 Salvar relatório Mochawesome como artefato
        if: always() # salva mesmo que algum teste falhe
        uses: actions/upload-artifact@v4
        with:
          name: mochawesome-report
          path: mochawesome-report/
