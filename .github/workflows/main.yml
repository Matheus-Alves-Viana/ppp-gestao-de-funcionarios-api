name: CI PPP Gestão de Funcionário

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: "0 3 * * *"

jobs:
  tests:
    runs-on: ubuntu-latest

    env:
      BASE_URL: 'http://localhost:3000'

    steps:
      - name: 📥 Checkout do repositório
        uses: actions/checkout@v4

      - name: 📦 Instalar dependências
        run: |
          npm install
          npm install --save-dev chai mocha supertest mochawesome

      - name: 🚀 Iniciar servidor e exibir logs em tempo real
        run: |
          nohup npm start > server.log 2>&1 &
          echo "⏳ Aguardando 10 segundos para o servidor iniciar..."
          sleep 10
          echo "===== INÍCIO DOS LOGS DO SERVIDOR ====="
          cat server.log || true
          echo "===== FIM DOS LOGS DO SERVIDOR ====="

      - name: 🧪 Executar os testes (permitir falhas)
        run: |
          npm test || echo "⚠️ Alguns testes falharam — gerando relatório mesmo assim."
        continue-on-error: true
        env:
          NODE_ENV: test

      - name: 📊 Publicar relatório Mochawesome
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mochawesome-report
          path: mochawesome-report/

      - name: 🛑 Encerrar servidor
        if: always()
        run: |
          echo "Encerrando servidor..."
          pkill node || true

      - name: 📋 Exibir status final
        if: always()
        run: echo "✅ Pipeline concluída — relatórios disponíveis."
